import React from 'react';
import styled from 'styled-components';

const H2 = styled.h2`
    color: #fff;
    font-size: 2rem;
    font-weight: 400;
`;
const NoResultFound = () => (
  <H2>
    No Result Found
  </H2>
);

export default NoResultFound;
